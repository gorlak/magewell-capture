# built native artifacts (regenerate via packages/magewell/build_lib.sh)
**/_lib/*.so

# python build / packaging
**/dist/
**/build/
*.whl
*.egg-info/
__pycache__/
*.py[cod]
.pytest_cache/

# environment
.venv/

# Claude Code
.claude/

# macOS
.DS_Store

# build scratch
**/linkstubs/

# local runtime data (gitignored — recreated at runtime)
sessions

# transfer destination — create this dir or symlink it to your storage target
storage
