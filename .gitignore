# built native artifacts (regenerate via packages/magewell/build_lib.sh)
**/_lib/*.so

# python build / packaging
**/dist/
**/build/
*.whl
*.egg-info/
__pycache__/
*.py[cod]

# environment
.venv/

# Claude Code
.claude/

# build scratch
**/linkstubs/

# local runtime data (gitignored — recreated at runtime)
sessions/

# local config (contains private paths; copy config.toml.TEMPLATE to get started)
config.toml
